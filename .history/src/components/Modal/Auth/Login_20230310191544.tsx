import React from 'react';

type LoginProps = {
    
};

const Login:React.FC<LoginProps> = () => {
    const [login, setloginFOrm]
    return (
        <form>
            {/* 2 inputs */}
        </form>
    );
}
export default Login;