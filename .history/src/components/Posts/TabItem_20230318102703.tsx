import React from 'react';
import { tabItem } from './NewPostForm';

type TabItemProps = {
    item:tabItem;
};

const TabItem:React.FC<TabItemProps> = ({ item }) => {
    
    return <div>{isTemplateExpression.}</div>
}
export default TabItem;